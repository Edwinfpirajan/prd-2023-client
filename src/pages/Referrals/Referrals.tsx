import styles from './Referrals.module.scss'

const Referrals = () => {
  return (
    <section className={ styles.referrals }>
      Lista de referidos por validar
    </section>
  )
}

export default Referrals